spring:
  datasource:
    url: jdbc:mysql://localhost:3306/file_info?useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
##### mybatis-plus打印完整sql(只适用于开发环境)
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

fs:
  isPreview: false
